import { Account } from "../../utils/types";
import { SetToken } from "../../utils/contracts";
export declare function reconcileBalances(setToken: SetToken, subject: any, signer: Account): Promise<void>;
